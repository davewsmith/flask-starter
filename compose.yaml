services:

  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 8000:5000
    environment:
      FLASK_APP: server
    command:
      - bash
      - -c
      - |
        flask run --host=0.0.0.0
